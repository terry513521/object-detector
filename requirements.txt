fastapi>=0.110
uvicorn[standard]>=0.27
opencv-python-headless>=4.8
numpy>=1.24
# Install onnxruntime-gpu on CUDA hosts (keeps GPU mode).
# On CPU-only hosts, swap to: pip install onnxruntime
onnxruntime-gpu==1.22.0
ultralytics>=8.0
pydantic>=2.0
python-multipart>=0.0.9
torch>=2.0
